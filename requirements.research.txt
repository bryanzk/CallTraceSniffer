# Research-only dependencies (not required for production deployment).
#
# Install:
#   python3 -m pip install -r requirements.research.txt
#
# Notes:
# - `graphviz` Python package typically still requires system `dot` binary.
#   macOS: brew install graphviz
#   Debian/Ubuntu: apt-get install graphviz

websockets>=12.0
graphviz>=0.20.0
