## 第 1 页

交易路径优化和执⾏编排的设计⽅案
MEVSolver:算⼦规格说明书(OperatorSpecifications)
编译流程:

```

编译流程:
输⼊: 原始交易图 (花瓣型)
↓
【本⽂档范围】IR 优化算⼦
├─ Op_1: 拓扑优化 (边降级)
├─ Op_2: 虚拟化
├─ Op_3: 形态标记
├─ Op_4: 原语展开
└─ Op_5: 结构修复
↓
输出: ExecutionGraph (IR)
├─ 优化的拓扑 (Edge.FlowType)
├─ AST 结构 (Node.Form, Payload)
└─ 执⾏顺序 (ExecOrder)
↓
【下⼀阶段】编码

```

数据结构定义(CoreDataStructures)
1.核⼼数据结构

```

// ExecutionGraph: 执⾏图 (中间表⽰ IR)
type ExecutionGraph struct {
Nodes map[NodeID]*SwapNode
Edges []*FundFlow
ExecOrder []NodeID // 执⾏顺序 (拓扑排序结果)
BotNode NodeID // Bot 节点 ID
PrincipalCap *big.Int // 本⾦上限
8
// 运⾏时状态 (仅⽤于 Op_5)

```


### 表格内容

| 交易路径优化和执⾏编排的设计⽅案
MEVSolver:算⼦规格说明书(OperatorSpecifications)
编译流程:

```

编译流程:
输⼊: 原始交易图 (花瓣型)
↓
【本⽂档范围】IR 优化算⼦
├─ Op_1: 拓扑优化 (边降级)
├─ Op_2: 虚拟化
├─ Op_3: 形态标记
├─ Op_4: 原语展开
└─ Op_5: 结构修复
↓
输出: ExecutionGraph (IR)
├─ 优化的拓扑 (Edge.FlowType)
├─ AST 结构 (Node.Form, Payload)
└─ 执⾏顺序 (ExecOrder)
↓
【下⼀阶段】编码 |

```

| --- |
| 数据结构定义(CoreDataStructures)
1.核⼼数据结构

```

// ExecutionGraph: 执⾏图 (中间表⽰ IR)
type ExecutionGraph struct {
Nodes map[NodeID]*SwapNode
Edges []*FundFlow
ExecOrder []NodeID // 执⾏顺序 (拓扑排序结果)
BotNode NodeID // Bot 节点 ID
PrincipalCap *big.Int // 本⾦上限
8
// 运⾏时状态 (仅⽤于 Op_5) |

```

<!-- 页面包含 4 个绘图对象 -->


---

## 第 2 页

10 RouterBalance map[TokenAddress]*big.Int // 模拟余额
11 }
Phase1:拓扑优化算⼦(TopologyOptimizationOperators)
Op_1:Deterministic_Direct(确定性直连)
功能定义
消除Router中转，将满⾜条件的资⾦流从 A → Router → B 优化为 A → B 。
接⼝签名

```

// 统⼀接⼝: Graph -> Graph 变换
func (op *Op1) Transform(graph *ExecutionGraph) (*ExecutionGraph, error)
3
// 返回: 修改后的新 Graph (原 Graph 不变，遵循不可变原则)
// 元数据通过 graph.Context.Logger 记录

```

判定条件(DecisionLogic)
对于每条边 E(A → B) (通过Router)，判定是否可直连：

```

Condition 1: 拓扑唯⼀性 (Topological Uniqueness)
Condition 2: 能⼒许可 (Capability Permission)
Condition 3: 资⾦覆盖 (Amount Coverage)

```

状态转换(StateTransformation)
Before(原始状态):

```

// Nodes
A: {ID: "uniV2_WETH_USDC", Out: 1000 USDC, CanRedirect: true}
B: {ID: "curve_USDC_DAI", In: 1000 USDC}

```

4


### 表格内容

| 10 RouterBalance map[TokenAddress]*big.Int // 模拟余额
11 } |
| --- |
| Phase1:拓扑优化算⼦(TopologyOptimizationOperators) |
| Op_1:Deterministic_Direct(确定性直连)
功能定义
消除Router中转，将满⾜条件的资⾦流从 A → Router → B 优化为 A → B 。
接⼝签名

```

// 统⼀接⼝: Graph -> Graph 变换
func (op *Op1) Transform(graph *ExecutionGraph) (*ExecutionGraph, error)
3
// 返回: 修改后的新 Graph (原 Graph 不变，遵循不可变原则)
// 元数据通过 graph.Context.Logger 记录

```

判定条件(DecisionLogic)
对于每条边 E(A → B) (通过Router)，判定是否可直连：

```

Condition 1: 拓扑唯⼀性 (Topological Uniqueness)
Condition 2: 能⼒许可 (Capability Permission)
Condition 3: 资⾦覆盖 (Amount Coverage)

```

状态转换(StateTransformation)
Before(原始状态):

```

// Nodes
A: {ID: "uniV2_WETH_USDC", Out: 1000 USDC, CanRedirect: true}
B: {ID: "curve_USDC_DAI", In: 1000 USDC}
|

```

<!-- 页面包含 14 个绘图对象 -->


---

## 第 3 页

5 // Edges
6 E1: {From: A, To: Router, Token: USDC, FlowType: Transfer, GasCost: 23k}
7 E2: {From: Router, To: B, Token: USDC, FlowType: Transfer, GasCost: 23k}
8
9 // Total Gas: 46k
After(直连优化后):

```

// Nodes (不变)
A: {ID: "uniV2_WETH_USDC", Out: 1000 USDC, CanRedirect: true}
B: {ID: "curve_USDC_DAI", In: 1000 USDC}
4
// Edges (合并 + 类型降级)
E_new: {From: A, To: B, Token: USDC, FlowType: Direct, GasCost: 5k}
7
// Total Gas: 5k (节省 41k)

```

性能特征
• 时间复杂度:O(E²)(E=边数)
• Gas收益:每次应⽤节省~41kgas
• 成功率:实际场景中约30%-50%的边可直连
Op_2:Virtual_Reduction(虚拟规约)
功能定义
消除ERC20Transfer，将Singleton内部的资⾦流降级为虚拟边（仅记账，⽆实际转账）。
接⼝签名

```

// 统⼀接⼝: Graph -> Graph 变换
func (op *Op2) Transform(graph *ExecutionGraph) (*ExecutionGraph, error)

```

判定条件(DecisionLogic)

```


### 表格内容

| 5 // Edges
6 E1: {From: A, To: Router, Token: USDC, FlowType: Transfer, GasCost: 23k}
7 E2: {From: Router, To: B, Token: USDC, FlowType: Transfer, GasCost: 23k}
8
9 // Total Gas: 46k
After(直连优化后):

```

// Nodes (不变)
A: {ID: "uniV2_WETH_USDC", Out: 1000 USDC, CanRedirect: true}
B: {ID: "curve_USDC_DAI", In: 1000 USDC}
4
// Edges (合并 + 类型降级)
E_new: {From: A, To: B, Token: USDC, FlowType: Direct, GasCost: 5k}
7
// Total Gas: 5k (节省 41k)

```

性能特征
• 时间复杂度:O(E²)(E=边数)
• Gas收益:每次应⽤节省~41kgas
• 成功率:实际场景中约30%-50%的边可直连 |
| --- |
| Op_2:Virtual_Reduction(虚拟规约)
功能定义
消除ERC20Transfer，将Singleton内部的资⾦流降级为虚拟边（仅记账，⽆实际转账）。
接⼝签名

```

// 统⼀接⼝: Graph -> Graph 变换
func (op *Op2) Transform(graph *ExecutionGraph) (*ExecutionGraph, error)

```

判定条件(DecisionLogic)

```

<!-- 页面包含 8 个绘图对象 -->


---

## 第 4 页

1 Condition 1: 必须已经是 Direct 边
2 Condition 2: 结算域⼀致 (Same Singleton)
3 Condition 3: 协议⽀持内部余额
状态转换(StateTransformation)
Before(直连状态):

```

// Nodes (都在 UniV4 Singleton 中)
A: {ID: "uniV4_pool_1", SingletonID: "0xUniV4Singleton", Out: 500 USDC}
B: {ID: "uniV4_pool_2", SingletonID: "0xUniV4Singleton", In: 500 USDC}
4
// Edges
E: {From: A, To: B, Token: USDC, FlowType: Direct, GasCost: 5k}

```

After(虚拟化后):

```

// Nodes (不变)
A: {ID: "uniV4_pool_1", SingletonID: "0xUniV4Singleton", Out: 500 USDC}
B: {ID: "uniV4_pool_2", SingletonID: "0xUniV4Singleton", In: 500 USDC}
4
// Edges (类型降级 + Gas 归零)
E: {From: A, To: B, Token: USDC, FlowType: Virtual, GasCost: 0}

```

性能特征
• 时间复杂度:O(E)
• Gas收益:每次应⽤节省~5kgas
• 前置依赖:必须在Op_1之后执⾏
Phase2:结构实例化算⼦(StructureInstantiationOperators)
Op_3:Mandatory_Scope(强制括号识别)
功能定义


### 表格内容

| 1 Condition 1: 必须已经是 Direct 边
2 Condition 2: 结算域⼀致 (Same Singleton)
3 Condition 3: 协议⽀持内部余额
状态转换(StateTransformation)
Before(直连状态):

```

// Nodes (都在 UniV4 Singleton 中)
A: {ID: "uniV4_pool_1", SingletonID: "0xUniV4Singleton", Out: 500 USDC}
B: {ID: "uniV4_pool_2", SingletonID: "0xUniV4Singleton", In: 500 USDC}
4
// Edges
E: {From: A, To: B, Token: USDC, FlowType: Direct, GasCost: 5k}

```

After(虚拟化后):

```

// Nodes (不变)
A: {ID: "uniV4_pool_1", SingletonID: "0xUniV4Singleton", Out: 500 USDC}
B: {ID: "uniV4_pool_2", SingletonID: "0xUniV4Singleton", In: 500 USDC}
4
// Edges (类型降级 + Gas 归零)
E: {From: A, To: B, Token: USDC, FlowType: Virtual, GasCost: 0}

```

性能特征
• 时间复杂度:O(E)
• Gas收益:每次应⽤节省~5kgas
• 前置依赖:必须在Op_1之后执⾏ |
| --- |
| Phase2:结构实例化算⼦(StructureInstantiationOperators) |
| Op_3:Mandatory_Scope(强制括号识别)
功能定义 |


<!-- 页面包含 6 个绘图对象 -->


---

## 第 5 页

根据协议特性进⾏AST转换，将Callback模式的节点标记为 ，保留Standard模式的节点
Scope
为 。
Node
接⼝签名

```

// 统⼀接⼝: Graph -> Graph 变换
func (op *Op3) Transform(graph *ExecutionGraph) (*ExecutionGraph, error)
3
// 返回: 修改后的新 Graph (所有节点的 Form 字段已设置)

```

判定条件(DecisionLogic)

```

func (op *Op3) DetermineForm(node *SwapNode) ExecutionForm {
switch node.Type {
case NodeTypeStandard: // V2, Curve
return FormNode
case NodeTypeCallback: // V3, V4, Balancer
return FormScope
case NodeTypeFlash: // Flash Loan
return FormScope
default:
return FormNode
}
}

```

状态转换(StateTransformation)
Before(平坦序列):

```

// ExecOrder
[A, B, C]
3
// Nodes (未设置 Form)
A: {Type: NodeTypeStandard, Form: nil} // UniV2
B: {Type: NodeTypeCallback, Form: nil} // UniV3
C: {Type: NodeTypeStandard, Form: nil} // Curve

```

After(形态标记):


<!-- 页面包含 12 个绘图对象 -->


---

## 第 6 页

1 // ExecOrder (不变)

```

[A, B, C]
3
// Nodes (设置 Form)
A: {Type: NodeTypeStandard, Form: FormNode, Payload: nil}
B: {Type: NodeTypeCallback, Form: FormScope, Payload: []} // 空 Payload
C: {Type: NodeTypeStandard, Form: FormNode, Payload: nil}

```

性能特征
• 时间复杂度:O(N)(N=节点数)
• Gas影响:⽆（纯分类，不改变执⾏逻辑）
• 前置依赖:⽆
Op_4:Primitive_Conversion(原语展开)
功能定义
将Scope的回调语义展开为明确的执⾏顺序，将抽象的Scope节点转换为三段式执⾏模式。
重要:这是IR层⾯的结构变换，不是最终的代码⽣成。输出仍然是ExecutionGraph，只是执⾏语义更
明确。
接⼝签名

```

// 统⼀接⼝: Graph -> Graph 变换
func (op *Op4) Transform(graph *ExecutionGraph) (*ExecutionGraph, error)
3
// 返回: 修改后的新 Graph，ExecOrder 已展开为明确的三段式顺序

```

展开语义(ExpansionSemantics)
对于Scope节点，其执⾏语义是三段式的：

```

Scope(B, Payload=[A, C]) 的执⾏语义:
1. Preamble: B 先乐观转账 (先给 Router 钱)
2. Body: 执⾏ Payload [A, C] (使⽤ B 的钱)
3. Postamble: B 回调时偿还 (Router 还钱给 B)

```


### 表格内容

| 1 // ExecOrder (不变)

```

[A, B, C]
3
// Nodes (设置 Form)
A: {Type: NodeTypeStandard, Form: FormNode, Payload: nil}
B: {Type: NodeTypeCallback, Form: FormScope, Payload: []} // 空 Payload
C: {Type: NodeTypeStandard, Form: FormNode, Payload: nil}

```

性能特征
• 时间复杂度:O(N)(N=节点数)
• Gas影响:⽆（纯分类，不改变执⾏逻辑）
• 前置依赖:⽆ |
| --- |
| Op_4:Primitive_Conversion(原语展开)
功能定义
将Scope的回调语义展开为明确的执⾏顺序，将抽象的Scope节点转换为三段式执⾏模式。
重要:这是IR层⾯的结构变换，不是最终的代码⽣成。输出仍然是ExecutionGraph，只是执⾏语义更
明确。
接⼝签名

```

// 统⼀接⼝: Graph -> Graph 变换
func (op *Op4) Transform(graph *ExecutionGraph) (*ExecutionGraph, error)
3
// 返回: 修改后的新 Graph，ExecOrder 已展开为明确的三段式顺序

```

展开语义(ExpansionSemantics)
对于Scope节点，其执⾏语义是三段式的：

```

Scope(B, Payload=[A, C]) 的执⾏语义:
1. Preamble: B 先乐观转账 (先给 Router 钱)
2. Body: 执⾏ Payload [A, C] (使⽤ B 的钱)
3. Postamble: B 回调时偿还 (Router 还钱给 B) |

```

<!-- 页面包含 6 个绘图对象 -->


---

## 第 7 页

Op_4将这个语义展开为扁平的执⾏顺序：

```

// Before (嵌套结构)
ExecOrder: [NodeX, Scope(B, Payload=[A, C]), NodeY]
3
// After (扁平化，语义明确)
ExecOrder: [
NodeX,
B_Preamble, // 标记: B 的乐观转账阶段
A, // Payload 执⾏
C, // Payload 执⾏
B_Postamble, // 标记: B 的偿还阶段
NodeY,
]

```

状态转换(StateTransformation)
Before(Scope嵌套结构):

```

// ExecOrder
[NodeA, ScopeB]
3
// ScopeB
B: {Form: FormScope, Payload: [C, D], TokenOut: USDC, AmountOut: 1000}
C: {Form: FormNode}
D: {Form: FormNode}

```

After(扁平化+阶段标记):

```

// ⽅案 1: 在 Node 上添加 Phase 标记
ExecOrder: [A, B, C, D, B]
Nodes: {
A: {Phase: Body}
B: {Phase: Preamble} // 第⼀次出现
C: {Phase: Body}
D: {Phase: Body}
B: {Phase: Postamble} // 第⼆次出现 (引⽤同⼀个 Node)
}
10
// ⽅案 2: 保持 Payload，但添加执⾏阶段元数据

```


<!-- 页面包含 6 个绘图对象 -->


---

## 第 8 页

12 ExecOrder: [A, B] // 不变
13 B: {
14 Form: FormScope,
15 Payload: [C, D],
16 ExecutionPlan: {
17 Preamble: {Action: "OptimisticTransfer", Token: USDC, Amount: 1000},
18 Body: [C, D], // 按顺序执⾏
19 Postamble: {Action: "Repay", Token: USDC, Amount: 1000},
20 }
21 }
推荐实现:保持结构，添加执⾏计划

```

// 在 SwapNode 中添加执⾏计划字段
type SwapNode struct {
// ... 原有字段 ...
Form ExecutionForm
Payload []NodeID
6
// Op_4 填充此字段 (仅当 Form=Scope 时)
ExecutionPlan *ScopeExecutionPlan
}
10
type ScopeExecutionPlan struct {
Preamble TransferAction // 乐观转账
Body []NodeID // 就是 Payload
Postamble TransferAction // 偿还
}
16
type TransferAction struct {
Type string // "OptimisticTransfer" or "Repay"
Token TokenAddress
Amount *big.Int
}

```

性能特征
• 时间复杂度:O(N)(N=节点数)
• Gas影响:每个Scope增加~2kgas(Preamble+Postamble)
• 前置依赖:必须在Op_3之后执⾏(需要Form和Payload)


<!-- 页面包含 4 个绘图对象 -->


---

## 第 9 页

Phase3:运⾏时修复算⼦(RuntimeRepairOperators)
Op_5:Engulfing(吞噬算⼦)
功能定义
偿付能⼒修复器，当模拟执⾏发现余额不⾜时，将缺钱的节点移⼊"富裕"的Scope节点的Payload
中。
接⼝签名

```

// 统⼀接⼝: Graph -> Graph 变换
// MaxDepth 通过 graph.Context.MaxDepth 配置
func (op *Op5) Transform(graph *ExecutionGraph) (*ExecutionGraph, error)
4
// 返回: 修改后的新 Graph (ExecOrder 和 Payload 已调整)
// 如果修复失败，返回 error

```

触发判定(TriggerLogic)
在模拟执⾏过程中：

```

func (op *Op5) SimulateAndDetect(graph *ExecutionGraph) (needRepair bool, prey
NodeID, token TokenAddress) {
balance := make(map[TokenAddress]*big.Int)
3
for _, nodeID := range graph.ExecOrder {
node := graph.Nodes[nodeID]
6
// 检查余额是否⾜够⽀付输⼊
if balance[node.TokenIn].Cmp(node.AmountIn) < 0 {
return true, nodeID, node.TokenIn // 发现偿付危机
}
11
// 更新余额
balance[node.TokenIn].Sub(balance[node.TokenIn], node.AmountIn)
balance[node.TokenOut].Add(balance[node.TokenOut], node.AmountOut)
}
16
return false, "", ""
}

```


### 表格内容

| Phase3:运⾏时修复算⼦(RuntimeRepairOperators) |
| --- |
| Op_5:Engulfing(吞噬算⼦)
功能定义
偿付能⼒修复器，当模拟执⾏发现余额不⾜时，将缺钱的节点移⼊"富裕"的Scope节点的Payload
中。
接⼝签名

```

// 统⼀接⼝: Graph -> Graph 变换
// MaxDepth 通过 graph.Context.MaxDepth 配置
func (op *Op5) Transform(graph *ExecutionGraph) (*ExecutionGraph, error)
4
// 返回: 修改后的新 Graph (ExecOrder 和 Payload 已调整)
// 如果修复失败，返回 error

```

触发判定(TriggerLogic)
在模拟执⾏过程中：

```

func (op *Op5) SimulateAndDetect(graph *ExecutionGraph) (needRepair bool, prey
NodeID, token TokenAddress) {
balance := make(map[TokenAddress]*big.Int)
3
for _, nodeID := range graph.ExecOrder {
node := graph.Nodes[nodeID]
6
// 检查余额是否⾜够⽀付输⼊
if balance[node.TokenIn].Cmp(node.AmountIn) < 0 {
return true, nodeID, node.TokenIn // 发现偿付危机
}
11
// 更新余额
balance[node.TokenIn].Sub(balance[node.TokenIn], node.AmountIn)
balance[node.TokenOut].Add(balance[node.TokenOut], node.AmountOut)
}
16
return false, "", ""
} |

```

<!-- 页面包含 4 个绘图对象 -->


---

## 第 10 页

吞噬策略(EngulfingStrategies)
策略A:前向吞噬(ForwardEngulfing)
适⽤场景：PredatorD在PreyN之前执⾏。
状态转换(Forward):

```

// Before
ExecOrder: [D(Scope), N(Node)]
D: {Payload: []}
N: {需要 100 USDC，但 Router 余额为 0}
5
// After
ExecOrder: [D(Scope)] // N 已移除
D: {Payload: [N]} // N 被吞噬

```

策略B:后向吞噬(BackwardEngulfing)
适⽤场景：PreyN在PredatorD之前（逻辑上），需要提升D为根。
状态转换(Backward):

```

// Before
ExecOrder: [N(Node), D(Scope)]
N: {需要 100 USDC，但 Router 余额为 0}
D: {Payload: [], 产出 200 USDC}
5
// After
ExecOrder: [D(Scope)] // D 提升到前⾯，N 移除
D: {Payload: [N]} // N 被吞噬

```

递归吞噬⽰例(RecursiveEngulfing)

```

// 初始状态
ExecOrder: [A(Node), B(Node), C(Scope)]
RouterBalance: 0
A: {需要 50 USDC}
B: {需要 100 USDC}

```


<!-- 页面包含 8 个绘图对象 -->


---

## 第 11 页

6 C: {Form: Scope, 产出 200 USDC}
7
8 // 第⼀轮吞噬
9 // A 缺钱 → C 吞噬 A
10 ExecOrder: [B(Node), C(Scope)]
11 C: {Payload: [A]}
12
13 // 第⼆轮吞噬
14 // B 缺钱 → C 再次吞噬 B
15 ExecOrder: [C(Scope)]
16 C: {Payload: [A, B]} // 最终结构
性能特征
• 时间复杂度:O(N²×A)(N=节点数,A=尝试次数)
• Gas影响:每次吞噬增加嵌套深度~2kgas
• 成功率:取决于是否存在合适的Scope节点
• 最坏情况:⽆法找到Predator，交易Revert
IR⽣成Pipeline(IRGenerationPipeline)
完整的IR⽣成流程（输出中间表⽰，不⽣成最终指令）：

```

func GenerateIR(initialGraph *ExecutionGraph) (*ExecutionGraph, error) {
// 初始化上下⽂
initialGraph.Context = &OperatorContext{
PoolRegistry: NewPoolRegistry(),
GasModel: NewGasModel(),
Logger: NewLogger(),
MaxDepth: 3, // 最⼤嵌套深度
}
9
// IR 优化算⼦链
operators := []Operator{
&Op1_DeterministicDirect{}, // Phase 1: 直连优化
&Op2_VirtualReduction{}, // Phase 1: 虚拟化优化
&Op3_MandatoryScope{}, // Phase 2: 形态识别
&Op4_PrimitiveConversion{}, // Phase 2: 语义展开
&Op5_Engulfing{}, // Phase 3: 偿付修复
}
18
// 函数式组合：graph -> op1(graph) -> op2(graph) -> ...

```


### 表格内容

| 6 C: {Form: Scope, 产出 200 USDC}
7
8 // 第⼀轮吞噬
9 // A 缺钱 → C 吞噬 A
10 ExecOrder: [B(Node), C(Scope)]
11 C: {Payload: [A]}
12
13 // 第⼆轮吞噬
14 // B 缺钱 → C 再次吞噬 B
15 ExecOrder: [C(Scope)]
16 C: {Payload: [A, B]} // 最终结构
性能特征
• 时间复杂度:O(N²×A)(N=节点数,A=尝试次数)
• Gas影响:每次吞噬增加嵌套深度~2kgas
• 成功率:取决于是否存在合适的Scope节点
• 最坏情况:⽆法找到Predator，交易Revert |
| --- |
| IR⽣成Pipeline(IRGenerationPipeline)
完整的IR⽣成流程（输出中间表⽰，不⽣成最终指令）：

```

func GenerateIR(initialGraph *ExecutionGraph) (*ExecutionGraph, error) {
// 初始化上下⽂
initialGraph.Context = &OperatorContext{
PoolRegistry: NewPoolRegistry(),
GasModel: NewGasModel(),
Logger: NewLogger(),
MaxDepth: 3, // 最⼤嵌套深度
}
9
// IR 优化算⼦链
operators := []Operator{
&Op1_DeterministicDirect{}, // Phase 1: 直连优化
&Op2_VirtualReduction{}, // Phase 1: 虚拟化优化
&Op3_MandatoryScope{}, // Phase 2: 形态识别
&Op4_PrimitiveConversion{}, // Phase 2: 语义展开
&Op5_Engulfing{}, // Phase 3: 偿付修复
}
18
// 函数式组合：graph -> op1(graph) -> op2(graph) -> ... |

```

<!-- 页面包含 4 个绘图对象 -->


---

## 第 12 页

20 graph := initialGraph
21 for _, op := range operators {
22 var err error
23 graph, err = op.Transform(graph)
24 if err != nil {
25 return nil, fmt.Errorf("operator %T failed: %w", op, err)
26 }
27 }
28
29 // 最终输出 (IR):
30 // - 优化后的拓扑结构 (graph.Edges with FlowType: Transfer/Direct/Virtual)
31 // - AST 形态 (graph.Nodes[*].Form, Payload)
32 // - 执⾏计划 (graph.Nodes[Scope].ExecutionPlan - 由 Op_4 填充)
33 // - 执⾏顺序 (graph.ExecOrder)
34 //
35 // 下⼀步: 短编码encoder消费这个 IR
36 return graph, nil
37 }
总结(Summary)
算⼦特性对⽐
算⼦ 类型 复杂度 Gas影响 失败可能 依赖关系
Op_1 优化器 O(E²) -41k ⽆ ⽆
Op_2 优化器 O(E) -5k ⽆ Op_1后执⾏
Op_3 解析器 O(N) 0 ⽆ ⽆
Op_4 展开器 O(N) +2k/层 ⽆ Op_3后执⾏
Op_5 修复器 O(N²A) +2k/层 有 Op_3后执⾏
关键⼯程考量
1. Op_1/2是纯优化：它们不依赖运⾏时状态，仅修改拓扑结构
2. Op_3是关键分界点：将协议特性转换为AST结构
3. Op_5是最后防线：仅在偿付危机时触发，有失败⻛险
4. 深度限制⾄关重要：超过3层嵌套会导致Gas爆炸


### 表格内容

| 20 graph := initialGraph
21 for _, op := range operators {
22 var err error
23 graph, err = op.Transform(graph)
24 if err != nil {
25 return nil, fmt.Errorf("operator %T failed: %w", op, err)
26 }
27 }
28
29 // 最终输出 (IR):
30 // - 优化后的拓扑结构 (graph.Edges with FlowType: Transfer/Direct/Virtual)
31 // - AST 形态 (graph.Nodes[*].Form, Payload)
32 // - 执⾏计划 (graph.Nodes[Scope].ExecutionPlan - 由 Op_4 填充)
33 // - 执⾏顺序 (graph.ExecOrder)
34 //
35 // 下⼀步: 短编码encoder消费这个 IR
36 return graph, nil
37 } |
| --- |
| 总结(Summary)
算⼦特性对⽐
算⼦ 类型 复杂度 Gas影响 失败可能 依赖关系
Op_1 优化器 O(E²) -41k ⽆ ⽆
Op_2 优化器 O(E) -5k ⽆ Op_1后执⾏
Op_3 解析器 O(N) 0 ⽆ ⽆
Op_4 展开器 O(N) +2k/层 ⽆ Op_3后执⾏
Op_5 修复器 O(N²A) +2k/层 有 Op_3后执⾏
关键⼯程考量
1. Op_1/2是纯优化：它们不依赖运⾏时状态，仅修改拓扑结构
2. Op_3是关键分界点：将协议特性转换为AST结构
3. Op_5是最后防线：仅在偿付危机时触发，有失败⻛险
4. 深度限制⾄关重要：超过3层嵌套会导致Gas爆炸 |


| 算⼦ | 类型 | 复杂度 | Gas影响 | 失败可能 | 依赖关系 |
| --- | --- | --- | --- | --- | --- |
| Op_1 | 优化器 | O(E²) | -41k | ⽆ | ⽆ |
| Op_2 | 优化器 | O(E) | -5k | ⽆ | Op_1后执⾏ |
| Op_3 | 解析器 | O(N) | 0 | ⽆ | ⽆ |
| Op_4 | 展开器 | O(N) | +2k/层 | ⽆ | Op_3后执⾏ |
| Op_5 | 修复器 | O(N²A) | +2k/层 | 有 | Op_3后执⾏ |


<!-- 页面包含 2 个绘图对象 -->


---

## 第 13 页

5. 模拟器精度决定正确性：Op_5依赖精确的余额模拟
IR的价值(ValueofIntermediateRepresentation)
⽣成的 IR包含：
ExecutionGraph
1. 拓扑优化结果( ):
Edges
◦ -需要通过Router中转
FlowType: Transfer
◦ -直连，节省~41kgas
FlowType: Direct
◦ -Singleton内部，0gas
FlowType: Virtual
2. AST结构( ):
Nodes
◦ -原⼦执⾏
Form: Node
◦ -回调作⽤域，包含 
Form: Scope Payload
◦ -嵌套的⼦节点ID列表
Payload
◦ -(Op_4填充)三段式执⾏计划
ExecutionPlan
3. 执⾏顺序( ):
ExecOrder
◦ 拓扑排序后的节点序列
◦ Op_5修复后的最终顺序
这套算⼦系统构成了⼀个完整的IR优化器，为后续短编码提供⾼质量的中间表⽰。


<!-- 页面包含 24 个绘图对象 -->
